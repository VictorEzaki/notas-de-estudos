enum Roles{
    ADMIN,
    User
}

Roles.ADMIN

// Os enums podemos referenciar valores às declarações. Por padrão elas são numéricas.

enum cargos {
    DESENVOLVEDOR = 'desenvolvedor',
    DESIGNER = 'designer'
}

cargos.DESENVOLVEDOR

// Podemos atribuir valores do tipo string na sua declaração.